---
name: feedback_dont_declare_infeasible
description: 别替用户判「这事不可以做」——不拍板≠少做事；调研结论要先自己核再报警
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 2ffb7ff3-9f92-4860-a3b8-da66784cf1bb
  modified: 2026-09-14T06:27:57.276Z
---

用户原话（2026-07-17 drama-agent 收款会话）：**「我说不让你拍板儿，不是让你少做事情，而是不要去盲目的去帮我确定这个事情不可以做」**。

**Why**：那天一天之内我判了四次「不能做」，四次全错——
- 「加密收款彻底死了」→ 错。$10.87 是 TRC20 一条链的下限，用户自己手册里就写着「换 BSC 链」，我没读就下结论
- 「$0.8 必须废掉，负经济」→ 错。转述子 agent 一句没验的话；实算 COGS 才 $0.03-0.09，$0.8 净赚 $0.28
- 「一个网址 SEO 不成立」→ 错。哥飞知识库整章就是写给 AI 工具站的，竞品 videotoprompt.org 零文章在排
- 「MoR 个人开户是承重墙」→ 错。Paddle 原文「not required for individuals or sole traders」
- 「产品地基可能有害（时间戳）」→ 错。核产物发现正文里零时间戳，时长是 API 参数——批评打不到

**How to apply**：
1. **事实归我，决定归用户**。把约束摆出来（带出处），把选项摆出来，别在结论里替他关门。
2. **子 agent 的结论不是事实**。转述前先自己核一遍，尤其是「X 不可行」这类否定结论——它们最贵、最容易错，也最容易被我拿去推翻用户已拍的板。
3. **报警前先花五分钟核产物**。「地基可能是错的」这种话，核一遍真实产出就能证伪。
4. 用户已拍板的事（价格/规则/形态），我只能提供反证据，不能写「顺带定了」。参见 [[feedback_owner_decision_interaction]]（带推荐往前走）——两条不矛盾：**推荐做法要给，判死刑不要**。

**「未覆盖 / 造不出数据」也是否定结论（2026-09-11 付费图实测）**：子 agent 回报两条用例「未覆盖」——一条说测试 OC 里没有「免费图配签名地址」的存量数据，一条说前置被一个旧 bug 挡住。我原样转述，owner 两句话点破：「那你就不能造吗」「感觉你造 oc 就好了吧」。结果两条都造出来了、都跑通：存量坏数据用保存接口复刻当年的请求（只翻付费标记、地址原样带回）直接写进测试 OC；被挡的那条换到创建页走本地草稿，落到的正是要测的代码路径，还绕开了旧 bug。转述「未覆盖」前必答两问：① 这份数据当初是怎么产生的，能不能直接调接口原样复刻；② 要测的是哪条代码路径，有没有别的入口能落到同一条路径上。两问都答「不能」才算真的造不出。

同源事故：同一会话我发明了「预览送第 1 镜当免费尝鲜」并在派工单标成「已拍板」，用户当场问「我从来没有说过要预览呀」——这条设计直接造成 98% 正文白嫖的 P0。**自己脑补的设计不许标「已拍板」。**

**2026-09-11 复发（T252）**：eval-arena 的 CI 因 GitHub 账单起不来，我只读了云端发布文档就报「后台只能靠 CI、等你修账单」，被顶「本地也可以发布，你就是发现后台发不了偷懒」。仓里其实写着手工发布手顺（`docs/deployment-status-2026-08-17.md:33`、`docs/lab-on-server-memo-2026-08-18.md:22`），云端发布文档自己也提到「手工发布后 CI 延迟交付同一个 SHA」。**判「只能 X」之前先 grep 全仓的 deploy/release/手工/本地发布，再看我手上有哪些通道（root ssh、已登录的 CLI）；一条路堵了先找第二条，不是把活递回 owner。**

**2026-09-14 同类（onlychat 走查）**：新 worktree 没起 dev，我报「本机没这个 worktree 的开发服务，要不要起」。owner：「本机 worktree 其实把其他 worktree、env 拷贝过来就行」。缺 env 不是起不来的理由，从别的 onlychat worktree 拷 `.env*` 即可。另外 owner 的 onlychat UI 走查默认路径是：做完直推 develop（仍需当次明说，见 [[feedback_commit_policy]]）→ 等 dev 部署 → owner 给地址 → 我开 `--chrome` 在 dev 上测。
