# 白名单模式：默认忽略一切，只放行可复用配置
# 排除掉的都是隐私/运行时状态：history.jsonl、projects(memory)、sessions、
# session-env、telemetry、cache、settings*.json（含机器路径/token）、mcp 凭据等
/*

!/.gitignore
!/README.md
!/agents/
!/skills/
!/commands/
!/hooks/
!/workflows/
!/design-spec/
!/CLAUDE.md
!/RTK.md

# 各目录内部的派生垃圾
__pycache__/
*.pyc
*.bak
.DS_Store
