# 制度维护协议 + 给未来 session 的信（harness-maintenance）

> 管的对象：`model-dispatch.md` / `judgment-rubrics.md` / `delegation-templates.md` / 本文件 / CLAUDE.md 里指向它们的路由行。
> 立法：Fable 5，2026-07-06。

## §1 权限分层——哪些改动可以自行做

**弱模型可自行改（改完 git commit，message 写清动机；不 push）**：
- 修错字、修死链/失效路径（前提：新路径去磁盘核实过真实存在）
- 更新事实表核对日期（前提：真的对照当下 harness 核过，不是顺手改日期）
- 给**既有**判准补充新的正例/反例，须带来源标注（格式见 §2）
- 模板措辞微调（不动结构、不动"验收条件"这个字段本身）

**先问用户才能动**：
- 删除或削弱任何既有规则/判准
- 改升降级阈值（错几次升级、重试几轮）
- 改 CLAUDE.md 里的路由指针
- 文件重组/合并/改名

**判定口诀：补例子、修事实 = 自行；动规则本身 = 先问。**

- ✅ 正例：发现 dispatch 里写的某 agent 类型已改名 → 核实新名后自行修正 + commit。
- ❌ 反例：觉得"重试 2 轮太保守"就自己改成 4 轮——阈值是规则本身，先问。

## §2 踩坑教训写回哪（分三层，别越级）

1. **一次性 / 项目性教训** → memory（`feedback_*` / `project_*`，走既有 memory 机制）
2. **跨项目、反复应验的判断规则** → 先落 memory 观察；用户点头"升制度"后才写进 `judgment-rubrics.md`。**不许跳过 memory 直接进制度文件**——制度文件收的是判过缓刑的惯犯，不是初犯。
3. **项目情报**（接口/部署/排障细节）→ 项目文档（既有铁律：项目情报不进全局 memory，更不进制度文件）

进制度文件的每条新例子标注来源：`[YYYY-MM-DD 事故/会话一句话]`。没有来源标注的例子，月度审计时视为可疑待清。

## §3 精简阈值

- 单个制度文件 **>300 行** → 下次月度审计时提议精简（合并重复例子、删已过时事实），列出删什么给用户过目
- 精简前先备份：`cp` 原文件到 `~/.Trash/`（本机铁律：绝不 `rm`，可逆优先）
- 节奏跟 `meta-memory-audit` 的月度审计合并做，别单开一摊

## §4 指针体检（月度，跟记忆审计一起）

- CLAUDE.md 路由行指向的 4 个制度文件都存在
- 制度文件里点名的 agent 类型 / 模型别名 / 工具参数与当下 harness 一致（对照 session 里的实时清单）
- 发现漂移：修事实层自行改（§1），涉及规则层先问

## 给未来 session 的信

### 三件没被问到、但对这个环境最重要的事

1. **这个环境最大的资产是"错误会被制度化"的闭环**：用户纠正 → memory → （反复应验后）制度文件 → 月度审计维护。你最容易犯的错是修完 bug 就走、不落教训——那等于让下一个 session 把同一个坑再踩一遍。每次被用户纠正，花 30 秒判断：要不要落 memory？（判据：下个 session 遇到同类事会不会再错。）

2. **用户的工作风格画像**（省得你从头试探）：自主推进，别反复请示——带 1 个推荐直接走，不列 A/B/C 让他挑；一切删除走废纸篓可逆；汇报要朴素精准，别黑话、别报菜名、别"今晚到这"式收尾；验证要看输出本体，不信管道摘要。拿不准语气时，宁可朴素。

3. **磁盘实况 > 一切记载**。memory、注释、文档、包括这套制度文件本身，都可能过时（本机判例：实验记录 ≠ 已采用；注释不是 ground truth）。判断技术栈看 lockfile；判断进度看文件内容指纹（不看 mtime）；执行制度前先核指针。冲突时：实况和用户明确指示优先，然后回来修文件。

### 这套制度最可能的退化方式与预防

| 退化方式 | 长什么样 | 预防（已内建在哪） |
|---|---|---|
| 指针腐烂 | 文件被移动/改名，CLAUDE.md 路由指到空处，制度静默失效 | §4 月度指针体检；改名必须同步指针（§1 先问项） |
| 规则通胀 | 每次踩坑加一条，文件胖到没 session 真读 | 新规则必须能说出它防止的**具体事故**，说不出就只配当 memory（§2）；>300 行触发精简（§3） |
| 仪式化空转 | 照抄模板但验收条件留空、审查 agent 为交差编 finding | 派工三件套缺一即失败（dispatch §3）；审查模板明写"0 finding 合法"（templates T5） |
| 过度派工 | 把 inline 小活也派出去，变慢变贵还显得很勤奋 | 派工下限排在 dispatch 第 0 节，就是防这个 |
| 品味自评 | 弱模型自己给品味打分，次品当良品交付 | rubrics §5 禁止品味自评，三选一必须明说 |

### harness 的极限（诚实条款，别假装制度能盖住）

拆解、验证、多样本评审补得了**执行品质**。补不了的：**模糊需求的裁量、纯品味判断、价值权衡**。遇到这三类，可选动作只有：升级模型、外部第二意见、交回用户——并且明说"这一层我判不准"。假装 rubric 能覆盖一切，是这套制度最体面的死法。

### 未完成交接

（本节留给中断的 session 写。2026-07-06 立法 session：无未完成项，A/B 两项系主动裁剪——已被 07.06 三场审计+修复覆盖，见 `~/Desktop/archives/技术总结/07.06-审计修复记录.md`。）
