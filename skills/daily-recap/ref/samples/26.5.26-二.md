26.5.26-二

- 世界卡 `onlychat-world-book`：首页 tag 组合态判错修（新增 `getHomeBrowseMode` 唯一判态 helper + proto 加 `hasWorldCard` 入参）、编辑器 scroll 状态外溢清理（PC 4 个 + mobile 2 个 sessionStorage key）、`worldCard.import` schema 加 `.nullable()` 走 `importWorldCardHandler`、`en.json` 里 `worldcard_error_upload_failed` 重复 key 删一个、mobile `addNote` 立刻 dirty/Save All Changes 常驻 修；走 cmm-pr 提到 `feature/lorebook`，PR 跟目标分支有 merge 冲突待解
- Loom/agg V0.0.6 `onlychat-agg-tuning`：搭出测试集 + 通用测试脚本（任意测试集可跑），§1.6 held-out 纪律从考生文档摘掉，执行预言机 `hardGate` 接进 scorer，定 100 分评分口径（含 UI 样式题）
- 分享 HTML 幻灯片 `~/Desktop/PRD/分享/claude-code-workflow-talk`：从 `_legacy/` 的 Slidev 备份迁到 HTML 版，主题 "vibe coding" 贯穿，串起 mk/sf-reader-all/skill 群/chat-cc-bot/agg

cc 日记（近 2 天）

- skills：改动 5 个 — `cmm-go`（去"说一声"+ watchdog 自启）、`cmm-pr`（加快冲档）、`gefei-seo`、`onlychat-tracking`、`onlychat-i18n`
- agents：无改动
- memory：新增 10 条 — `watchdog_user_switches_self`、`real_requirement_archive`、`xiaohongshu_idea_dump`、`daily_recap_automation`、`worldbook_dev_oom`、`onlychat_i18n_format`、`no_coupling_instinct_in_flat_domains`、`dont_litigate_immaterial_caveats`、`plan_before_code`（方向：watchdog 异位只提示 / 落档路径 / 小红书改产物到 HTML / OOM 用 dev-watchdog 保活 / i18n 4-space 转义格式 / 扁平域机械去重 / 别揪 immaterial caveat / 写代码前先出方案）
- hooks：`settings.json` 5/26 12:24 改过一次
