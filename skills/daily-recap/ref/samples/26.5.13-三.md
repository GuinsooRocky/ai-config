26.5.13-三

- onlychat 世界卡（主线）：新建两个 worktree `onlychat-agg-tuning` 和 `onlychat-world-book`（基于 develop 拉最新），并行跑世界卡 1.0 / 2.0 开发任务
  - 任务覆盖：PRD §3.4 图像上传组件、§3.1 世界卡卡片组件、§8.1 创建入口
  - 接口从 mock 转正式：`onlychat-agg-tuning` 这边 develop 已 merge 后端字段，切到真接口跑 `3000` 调试
  - 学习 4 层异步 + 回退（本地预览→压缩→裁剪→上传，每步失败回退 + 本地小版控回收）的图像上传机制，落档到 `~/Desktop/cmm/onlychat-World-Path/`
- fe-workflow QA 环节改造（meta 工作流）：QA 追问要精准映射 PRD 章节号（无索引则贴原文 + 行数），并把追问产物从对话里搬出来 HTML 落桌面，结束自动 rm
- analyzer skill 升级：触发收紧到微信 / 小红书 / X / YouTube / B站，通用网页不抢路由；同步 push 到 `GuinsooRocky/sf-reader-all`
- 小红书：翻了已存素材并更新；让 `khazix-writer` 出 HV-Alices 版本（大横向跨度对比 T0–T3 公司路线）做转发去 AI 味测试

你自己再调一调。
