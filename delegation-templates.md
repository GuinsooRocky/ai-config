# 派工模板（delegation-templates）

> 用法：主对话派子 agent 时，抄对应模板、填 `____`、整段放进 Agent 的 prompt 参数。
> 填不出「验收条件」= 你还没想清楚这活怎么算完——先想清楚再派；或者它根本是个该 inline 的小活（见 `model-dispatch.md` §0，别派）。
> model 建议按 `model-dispatch.md` §2；省略 model = 继承主会话模型，日常活通常省略即可。
> 立法日期：2026-07-06。

## 公共尾巴（每份模板末尾都带，别删）

```
## 回报格式（必须照此回）
- 结论：≤5 行
- 关键锚点：文件:行号（每条发现都要）
- 长产物（>50 行）：写到 ____（没指定就写 scratchpad），只回路径
- 没做 / 没查到的部分：明说 + 报覆盖率 X/N，绝不静默略过
- 机器判断的结论（测试过/抓完了）：附命令 + 关键输出摘录，裸的"通过了"不算
```

---

## T1 搜索 / 定位（subagent_type: Explore；model: 省略继承）

```
在 ____（目录/仓库）里找出 ____（模式/符号/配置/文案）的所有出现。
背景：我要 ____（动机一句话，帮你在模糊命中时做取舍）。
范围：包含 ____；排除 node_modules、构建产物、____。
搜索宽度：____（medium = 常规 / very thorough = 多目录多命名穷尽）。
验收条件：
- 给出全部命中清单（文件:行号），不确定的命中单独标出
- 报覆盖率：扫了哪些目录/命名变体
- 0 命中也是合法结果——但要列出你试过的关键词组合
+ 公共尾巴
```

## T2 实作（subagent_type: general-purpose；model: 日常省略，难度高→opus）

```
实现：____（一句话目标）。
动机：____（做给谁用、解决什么，一句话）。
上下文锚点：相关文件 ____；要遵循的现有模式 ____（先读再写，别自造轮子）。
第一步先核实：重读上述锚点，确认文件/签名/模式还是我说的这样；漂了就停下回报，别硬做。
边界（违反即失败）：
- 只改 ____ 范围内；不顺手重构/美化相邻代码
- 不碰 ____（如：配置文件、其他模块）
验收条件（全部满足才算完）：
- ____（可机械判定：某命令通过 / 输出包含 X）
- 真实触发路径走过：____（怎么触发、预期看到什么）
- 边界态验过：空态 / 超长 / ____
+ 公共尾巴（另加：改了哪些文件:行号，每处一句为什么）
```

## T3 重构（subagent_type: general-purpose；model: 单模块省略，跨模块→opus）

```
重构 ____，目标：____（一句话，如"消除 X 的重复实现"）。
不变量（违反即失败）：外部行为不变；____ 的公开接口不变；不改无关代码。
第一步先跑基线：执行 ____（测试/typecheck/某命令），记录结果——重构后同命令结果必须一致。
验收条件：
- 基线命令重构前后结果一致（贴两次输出对照）
- diff 里每个改动都能对应到重构目标，说不出对应关系的改动 = 越界，撤掉
+ 公共尾巴（另加：基线前后对照）
```

## T4 研究 / 调研（subagent_type: general-purpose；model: 收集省略，综合判断重→opus）

```
调研：____（问题）。
动机 / 要支撑的决策：____。
来源范围：____（给定的 URL/文件/目录，枚举全部，报覆盖率 X/N，绝不静默丢任何一个）。
验收条件：
- 每个结论都有出处锚点（URL / 文件:行号）
- 区分"来源说的" vs "你的推断"，分开标
- 查不到的明确标"未查到"，不许编造
产物：写到 ____，对话里只回 3–5 行摘要 + 路径。
+ 公共尾巴
```

## T5 审查（代码 bug 用 bug-hunter→adversary→judge 三件套；一般产出如下；model: opus）

```
你是 fresh-context 审查者，没参与生产过程，只对结论负责。
审查对象：____（文件/diff/报告的路径）。
审查维度：____（按需选：规则互相矛盾 / 引用的路径与名称是否真实存在 / 弱模型会误读的模糊句 / 判准缺正反例 / 逻辑漏洞）。
硬规则：
- 每条 finding 锚 文件:行号 + 一句具体失败场景（谁在什么情况下会因此出错）
- 点名的路径/工具名/skill 名，逐个去磁盘核实存在性后才能定为"死引用"
- 拿不准的标 PLAUSIBLE，别硬凑数量；0 finding 是合法结果，别为交差编
验收条件：对象全文读完（报读了哪些部分）；每条 finding 给出修复建议。
+ 公共尾巴
```

---

## 速取索引

| 你要干的事 | 模板 | agent | model |
|---|---|---|---|
| 找东西在哪 | T1 | Explore | 省略 |
| 写功能/修 bug | T2 | general-purpose | 省略 / opus |
| 改结构不改行为 | T3 | general-purpose | 省略 / opus |
| 查资料出结论 | T4 | general-purpose | 省略 / opus |
| 挑毛病 | T5 | general-purpose（代码用三件套） | opus |
