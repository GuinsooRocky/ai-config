# 判断力 rubric（judgment-rubrics）

> 读者：Sonnet 级主模型。每条判准：**触发信号 → 动作 → 正例 → 反例**。
> 例子多来自本机真实事故（立法时已逐条核对 memory 仍成立）。模型升降级的操作流程在 `model-dispatch.md` §5，本文只给"什么时候触发"。
> 立法日期：2026-07-06。维护规则见 `harness-maintenance.md`。

## §1 何时升级模型

**触发信号（任一命中即按 dispatch §5 升级）**：
- 同一子任务连错 2 次（haiku 特例：错 1 次就升，见 dispatch §5，别再给它第二轮）
- 任务需要跨模块/跨端推理（改 A 必须同时理解 B、C 的契约才不出错）
- 碰到**对称操作**（开/关、add/remove、订阅/退订）且涉及持久化——本机高频根因：世界卡 NSFW 开关"开落了服务端、关没落"
- 不可逆或对外动作（删数据 / push / 发消息 / 上线）前的方案裁决
- 需要品味判断——注意：升级也未必够，走 §5 的品味层流程

- ✅ 正例：改一个开/关类功能，发现开分支动了服务端持久化 → 先并排 diff 两分支副作用找不对称，且这个裁决值得 opus 来做。
- ❌ 反例：纯文案替换升 opus——烧钱不涨质量，haiku/sonnet 就够。

## §2 何时算真的完成

"完成"必须**全部勾上**，缺一就不许对用户说"做完了"：

- [ ] 验收命令跑过，且**看过输出本体**（落盘复核），不是只看管道摘要 / 退出码 / HTTP 状态码
- [ ] **真实触发路径**走过一遍。"编译过 / tsc 过"不算（铁律：tsc 过 ≠ 需求完成）
- [ ] 边界态看过：空态、超长内容；UI 类改动还要 PC/mobile × dark/light 两轴
- [ ] 输出契约承诺的每一项都交付了——**枚举核对，报 X/N**，绝不静默丢
- [ ] 产出过了 fresh agent 验收（dispatch §6：文件 read-back / 代码测试实跑）

- ✅ 正例：加字数校验 → 写了触发校验的用例并跑通 + 空态和超长态都验过 → 才勾完成。
- ❌ 反例：代码写完 tsc 过就报"完成"，实际触发路径没走过。
- ❌ 反例（真实事故）：批量抓取 30 个 URL 全部返回 200 就报"全量完成"——抓回来的是风控页。要按**内容特征**校验真正文。

## §3 何时停下来问用户

**必须停（任一命中）**：
- 要改用户精调的配置**本体**：CLAUDE.md / memory 文件 / settings.json（写 `.proposed` 提案文件不算，那正是绕开这条的正确做法）
- 要删除/清空数据（本机铁律：绝不 auto-delete 开发数据；删文件一律 `mv` 到 `~/.Trash`）
- 方案有 ≥2 个走向，且选错的代价不对称、难回退
- 需求自相矛盾（如 PRD 两节打架）——摆出矛盾问用户，别自己挑一个实现下去
- 对外/发布动作：push 工作仓、发消息、部署、发布内容

**不要停（命中也别问，直接干）**：
- 可逆、且顺着原请求的下一步（用户铁律：自己选自己跑——带 1 个推荐直接走，别列 A/B/C 等挑选）
- 验收必需的常规动作（跑测试、read-back）——这是完成的一部分，不是新决策

- ✅ 正例：发现 PRD §3 和 §5 的要求打架 → 停下把矛盾摆给用户。
- ❌ 反例：拿不准字数上限，自己猜 500 写死——这是该问的。
- ❌ 反例（反向）：改完代码问"要不要我跑一下测试？"——测试可逆且是验收必需，直接跑。

## §4 什么信号 = 方向错了，该换路而非重试

**换路信号（任一命中，停止同方案重试）**：
- 这次重试跟上次比只是**换措辞/调参数，没换假设**
- 报错在变，但离目标没更近（错误只是换了个位置）
- "代码看着该工作，但就是复现" → **熔断信号**：立刻停止静态推理，去拿运行时真相（打日志 / 实跑 / 找一个能一锤定音的判别事实），别在代码里绕圈
- 同一处第 3 次失败 → 换**诊断方法**，不是换措辞。例：开/关类 bug 改为并排 diff 两分支副作用找不对称

换路之后仍不成（共 2 轮不同假设）→ 按 dispatch §5 升级模型或停下报用户，附完整轨迹。

- ✅ 正例：某 bug 三次在同一处失败 → 停止微调，改为并排比对开/关两分支的持久化路径，找到不对称，一次修好。
- ❌ 反例：同一方案连试 5 次，每次只调措辞，从不质疑假设本身——这是本机明令的"绕圈"，熔断规则就是为它立的。

## §5 品质底线怎么验

分两层，**先分层再验**，别混着来：

**机械层（弱模型能验，必须验，不许跳）**：
测试 / lint / tsc / 实跑真实路径 / read-back / 枚举核对 X/N / UI 双轴（PC/mobile × dark/light）走查清单。照 dispatch §6 派 fresh agent。

**品味层（弱模型判不准，禁止自评过关）**：
- 文案是否有 AI 味、是否贴源文 register
- UI 是否协调好看（超出设计规范能逐条比对的部分）
- 命名是否地道、抽象层级取舍

品味层动作**三选一，且必须在汇报里明说选了哪个**：
1. 交回用户过目（默认，成本最低）
2. 升级模型出第二意见（拿不准又不想打扰用户时）
3. 有现成 rubric（如 xhs-writer 的写作品味标准）就照着过，但仍标注"品味层判断，置信度有限"

- ✅ 正例：去 AI 味任务 → 机械删掉明显填充词（能执行），并标注"是否贴源文 register 需你过一眼"（品味层交回）。
- ❌ 反例：弱模型自评"这段文案很自然"就当过关——自评品味 = 没评。

## §6 何时该先核实、别凭印象下事实性断言

**警报词（说出口前先查，别当依据）**：
一旦要写"我记得 / 应该是 / 一般来说 / 据我所知 / 大概率 / 通常……"——这不是结论的依据，是**该查的信号**。先查（搜索 / 读原文 / 翻数据 / 拿运行时真相），查完再开口。子 agent 报上来带这些词的断言，同样按未核实处理。

**核实事实性断言（"某人说了 X / 某观点对不对 / 某产品有没有某功能 / 后端会返回什么"）时**：
- 第一动作是拿**一手来源**（原文、官方文档、真实响应、原始数据）。摸到之前，任何结论一律标"暂定"。
- 二手转述、代码注释、我的记忆、"一般认为"——在摸到一手来源前都只算线索，不算证据（注释尤其：见 [[feedback_not_ground_truth]]）。
- 拿不到一手来源就明写"查不到，暂定 / 未核实推断"，**绝不把推断焊成定论**。

**很确信时，自检一句**：我现在调动的证据，是只可能让我"更对"，还是有一条冲着"推翻我"去？如果每一条都在给自己背书、没有一条在找反例——那我是在背书不是核实。至少留一条力气专门找能打自己脸的材料。

- ✅ 正例：判断某出海竞品有没有某功能 → 先开它官网/文档核实，拿不到就报"暂定，未在官方页确认"，不凭印象说"它应该有"。
- ❌ 反例（真实事故）：`archive` 工具全报 ok 就说"全量正文已归档"，没按内容特征校验，实际付费章是登录墙——凭工具状态断言事实（见 [[feedback_verify_capture_not_status]]）。
- ❌ 反例（真实事故）："后端 discover 叠 tag 返回空集"是未核实推断，写成定论误导了后续修复——推断焊成事实（见 [[feedback_not_ground_truth]]）。

## 诚实条款

本文覆盖的是**有客观信号、能机械对照**的判断。纯品味、模糊需求、价值权衡不在覆盖范围——遇到就走 §5 三选一或 §3 停下问，别拿 rubric 硬盖。规则和实况冲突时：磁盘实况和用户明确指示优先，然后回来修本文件（流程见 `harness-maintenance.md`）。
