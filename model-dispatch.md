# 模型调度守则（model-dispatch）

> 读者：未来每个 session 的主模型（Sonnet / Opus / Haiku）。
> 事实表核对日期：**2026-07-06**（Fable 5 立法 session 当日对照 harness 实况核对）。
> 凡本文引用的型号别名 / agent 类型 / 工具参数：如与你当下 harness 不符，**以你现查为准**，修正后更新上面的日期（改法权限见 `harness-maintenance.md`）。

## §0 派工下限——这些活别派，主对话直接干

派 agent 有固定开销（等待 + 上下文交接损耗），小活派工是净亏损。

**直接 inline 干（不派）**：
- 读 ≤3 个已知路径的文件
- 跑一条命令看结果
- 单文件、改法已明确的小修改
- 回答用户问题所需的单点查证（一次 grep 能定位的）
- 判断类小活同理：文件已知且 ≤3 个，读逻辑判真伪也 inline；要跨多文件/多步推理才够格派工 [2026-07-06 实测校准]

**派工才划算**：
- 不知道目标在哪，要扫多目录 / 多种命名 → 派 `Explore`
- 多步任务、过程会产生大量你只需要结论的中间读取 → 派 `general-purpose`
- 需要互相独立的多视角（审查、对抗、评审）→ 派多个 agent
- 判定标准一句话：**这个动作会往主对话灌 >200 行你只需要结论的内容吗？会 → 派；不会 → 自己干。**

- ✅ 正例：在 40 个文件里找某模式的所有出现 → 派 `Explore`，主对话只收"命中清单 + 文件:行号"。
- ❌ 反例（真实事故 2026-05-29）：查一个事实开了后台 workflow，空转 51 分钟。查事实、读文件、跑命令这种小活，inline 几十秒就完。

## §1 指挥官不下场

主对话是编排者和裁决者，不是搬运工。大量读取 / 扫 repo / 查网页 / 批次改档，一律派子 agent，主对话只收结论。主对话上下文是全 session 最贵的资源——灌满了文件全文，后半场就失焦。

- ✅ 正例：要理解一个陌生子系统 → 派 `Explore` 回"模块地图 + 关键文件:行号"，主对话基于地图做决策。
- ❌ 反例：主对话自己逐个 Read 40 个文件，把全文糊进上下文，烧 token 又把早先的用户指令挤出注意力。
- ✅ 边界补充（文档/配置综合类活的下场判据）：素材已在主对话上下文 **且** 产出是拍板/品味档（方案取舍、golden sample）→ 主模型亲写反而省——派出去等于让子 agent 重读全部素材、主对话再逐行复核它的取舍；反之，改法能列成 change spec 的（skill/配置重构），spec 归主对话，执行派 `sonnet`，验收走既有关卡。[2026-07-07 出海SEO会话：用户点破 skill 重构不该主模型亲写；改为 spec+sonnet 执行，一次过 meta-check 100]

## §2 可用模型与选型速查

Agent 工具 `model` 参数只认这四个别名（完整 ID 供写文档引用）：

| 别名 | 完整 ID | 定位 | 派它干什么 |
|---|---|---|---|
| `haiku` | claude-haiku-4-5-20251001 | 最便宜最快 | 机械活：批量套已解出的模式、格式转换、简单提取、大批文件同一改法 |
| `sonnet` | claude-sonnet-5 | 主力 | 日常实作 / 搜索 / 读取 / 写码 / 绝大多数子 agent 活 |
| `opus` | claude-opus-4-8 | 难活 | 复杂架构判断、难 bug、跨模块推理、高风险综合、当裁判/评审 |
| `fable` | claude-fable-5 | 一次性最高判断 | 立法窗口期专用，日常调度不要依赖它可用 |

**effort 事实（2026-07-06 核对，别写反）**：
- **Agent 工具没有 `effort` 参数**，只有 `model`。在 Agent 调用里传 effort 会失败。
- effort（`low`/`medium`/`high`/`xhigh`/`max`）只有两个落点：Workflow 脚本里 `agent()` 的 opts，和 `.claude/agents/*.md` frontmatter。
- 省略 `model` = 子 agent 继承主会话模型，**这通常是对的**；只有任务明确匹配下表才显式指定。

任务型态 → 选型速查：

| 任务型态 | model | effort（仅 Workflow / frontmatter 可给时） |
|---|---|---|
| 批量套已解出的模式 / 格式转换 / 机械提取 | `haiku` | low |
| 日常搜索 / 读取 / 实作 / 写码 | `sonnet`（或省略继承） | medium |
| 复杂架构 / 难 bug / 跨模块 / 评审裁决 | `opus` | high |
| 最终对抗验证 / 多答案评审选优 | `opus` | xhigh |

- ✅ 正例：把已确认的改法套到 30 个文件 → `haiku`。
- ❌ 反例：拿 `opus` 做"把这个字符串替换成那个"——又慢又贵，质量不涨。

## §3 派工三件套（缺一即失败）

每次派 agent，prompt 里必须齐这三样。填不出第 ② 条，说明你自己还没想清楚——先想清楚，或者它就是个该 inline 的小活（§0）。

1. **目标与动机**：干成什么、为什么（子 agent 拿动机才能在边界情况下做对取舍）
2. **验收条件**：怎么算完，**可机械判定**（命令输出、覆盖率 X/N、文件存在且含某节）
3. **回报格式**：回什么结构（见 §4）

现成模板在 `~/.claude/delegation-templates.md`，按任务型态抄。

- ✅ 正例：见 delegation-templates.md T2 的完整实作派工。
- ❌ 反例："帮我看看 X 有没有问题"——没验收条件、没回报格式，子 agent 只能瞎发挥，回来的东西没法用。

## §4 回报合约

派工 prompt 里必须写明（子 agent 不会自己知道）：
- 只回：**结论（≤5 行）+ 关键锚点（文件:行号）**
- 长产物（>50 行的报告 / 清单 / 代码）：**落盘，只回路径**，绝不把全文糊回主对话
- 没做 / 没查到的部分：明说，不许静默略过（报覆盖率 X/N）
- **机器判断的结论必须附证据**：说"测试通过"要附命令 + 关键输出摘录或落盘路径；说"抓取完成"要附内容特征核验结果。裸的"通过了/完成了"不算数——本机管道摘要出过假阴性（2026-07-02 ps 假阴性），状态码 200 抓回来是风控页的坑也踩过。

- ✅ 正例：子 agent 回"发现 3 处不对称持久化，最狠一条在 `foo.ts:42`；完整审计报告落在 `<路径>`"。
- ❌ 反例：子 agent 把 2000 行审计报告全文贴回主对话。

## §5 升降级路径

- **haiku 错 1 次** → 直接升 `sonnet` 重做：把**原任务原样**给 sonnet 即可，**不必附 haiku 的失败轨迹**（它错是能力不够，轨迹无诊断价值——这点与 sonnet→opus 必须带轨迹相反，别混）。不在 haiku 上磨 prompt。[2026-07-06 实测校准]
- **sonnet 同一子任务连错 2 次** → 带**完整失败轨迹**升 `opus`。轨迹 = 目标 + 两次分别怎么改的 + 报错/错误输出原文。不带轨迹等于让 opus 从零猜，白升。
- **贵模型解出根因/模式后** → 降回 `sonnet`/`haiku` 批量套用到同类处。贵模型不做搬砖。
- **同一件事（同一假设）最多重试 2 轮**，还不成 → 停下报用户，附轨迹。换假设算新的一件事——但什么时候该换假设而非重试，见 `judgment-rubrics.md` §4。
- 总预算公式（防按字面读窄 [2026-07-06 实测校准]）：同一假设 ≤2 轮 → 换假设再 ≤2 轮 → **累计 2 个假设都失败** → 升级模型或停下报用户。不是"总共只许试 2 次"。

- ✅ 正例：sonnet 两次都修不好某异步竞态 → 把两次 diff + 报错打包给 opus；opus 给出根因后，由 sonnet 把修法套到同类的另外 5 处。
- ❌ 反例：sonnet 在同一个 bug 上试第 6 次，每次只换措辞，轨迹从没升级过——token 烧完了，bug 还在。

## §6 验证不自验

**谁生产的，谁就不能给自己验收。** 验收一律派 fresh-context agent（没参与过生产的干净上下文）：

| 产出类型 | 验法 |
|---|---|
| 文件 / 文档 | read-back：干净 agent 重新读回，核对存在、非空、覆盖约定的每一节 |
| 代码 | 测试或实跑**真实触发路径**。"编译过 / tsc 过"不算完（本机铁律：tsc 过 ≠ 需求完成） |
| 高风险判断 | 第二意见（独立 agent 重判）或多答案评审（生成 N 个，派裁判挑）。代码审查用现成三件套：`bug-hunter` → `adversary` → `judge` |

- ✅ 正例：sonnet 写完改动 → 主对话另派一个全新 `general-purpose` 跑测试 + read-back，通过才算完。
- ❌ 反例：写代码的那个 agent 自己说"我验证过了没问题"就收工。

## §7 编排硬事实（写反会翻车）

- **子 agent 不能再 spawn 子 agent**。扇出、流水线这类编排一律主对话做，别指望子 agent 自己拆。
- **Workflow 工具只在用户显式要求多 agent 编排时用**（说了 ultracode / "用 workflow" / 调用了要求 Workflow 的 skill）。自作主张开 workflow 违反 harness 规则，且有空转前科。
- 并行改文件**会互相冲突**时才用 `isolation: 'worktree'`（每个 agent 约 200–500ms 建树 + 磁盘开销）；只读或不冲突就别用。
- 多个互相独立的派工，放**同一条消息**里并行发。
- 现成 agent 类型以你 session 里 system reminder 的实时清单为准（本文写作时含：`Explore` / `Plan` / `general-purpose` / `bug-hunter` / `adversary` / `judge` / `visual-qa` / `mock-data-builder` 等）。派前确认类型存在，别凭记忆写。

## 诚实条款

这份守则补得了**执行品质**：派工纪律、模型选型、验证流程。补不了**模糊题与品味判断**——什么算"好文案"、方案取舍这类，rubric 到顶也只是及格线，处理方式见 `judgment-rubrics.md` §5（升级模型 / 第二意见 / 交回用户，三选一并明说）。
